server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Barcha so'rovlarni index.html ga yo'naltirish (SPA uchun eng muhimi shu qator)
        try_files $uri $uri/ /index.html;
    }
}